\ProvidesPackage{khalidUrduBooksEMT}
\usepackage{fontspec}
\usepackage{makeidx}
\usepackage{amsmath}                                                                         %\tfrac for fractions in text
\usepackage{mathtools}
\usepackage{commath}									%differentiation symbols
\usepackage{bm}											%bold greek letters too
\usepackage{lineno}										%line numbers in pdf
\usepackage{graphics}
\usepackage[]{graphicx} 
\usepackage{cancel}
\usepackage{mathpazo}              %kkk
\usepackage{booktabs}                                  %for tables toprule, midrule and bottomrule
%\usepackage{parskip}
\usepackage[stable]{footmisc}						%for footnotes in the headings

\usepackage{siunitx} 			%SI units that meets iso-31 regulations such as \micro, \degree \SI{3}{\celsius}
\sisetup{per=frac,fraction=nice}    %setup for siunitx

\protected\def\numpi{\text{\ensuremath{\pi}}}  %25 june 2015, these two lines were added to be able to use numpi as pi in SI commands.
\sisetup{input-symbols = \numpi}					%without it, in the presence of mathpazo package, SI would show beta instead of pi

\usepackage{steinmetz}		%\phase to show angle of phasors
\usepackage[version=3]{mhchem}     %chemical symbols such as \ce{H2O}


\usepackage{geometry}                        %the geometry package gives correct page size in pdf meta data i.e. pdf file properties
%\pdfpagewidth=\paperwidth              %instead of the geometry package, could have used these two lines      
%\pdfpageheight=\paperheight

% \usepackage{microtype}               %underfull messages try using it if needed. check first if it works with xelatex

\usepackage{makeidx}                %  \makeindex 
\usepackage{placeins}                   % \FloatBarrier

\usepackage{caption}                               %must be placed before bidi
\usepackage{subcaption}

%\usepackage{hyperref}                   %loaded second last just before polyglossia. still giving problems
\usepackage{polyglossia}                %is loaded the last


%\renewcommand\thelinenumber{\textenglish{\arabic{linenumber}}}
%\linenumbers												%for line numbers in pdf

\setmainlanguage[numerals=maghrib]{arabic}     %for english numbers use numerals=maghrib, for arabic numerals=arabicdigits
\setotherlanguages{english}


\newfontfamily\arabicfont[Scale=1.0,Script=Arabic]{Scheherazade}
\newfontfamily\urdufont[Scale=1.0,Script=Arabic]{XB Tabriz}

%\newfontfamily\AcaciasHand{AcaciasHand}

\setcounter{secnumdepth}{3} 

